# production compose with postgres storage, cloudflared tunnel and logging
services:
  tg-spam:
    build:
      context: ..
      dockerfile: Dockerfile
    image: tg-spam:local
    hostname: tg-spam
    container_name: tg-spam
    restart: always
    logging: &default_logging
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
    env_file:
      - .env
    volumes:
      - ./logs:/srv/logs
    ports:
      - "127.0.0.1:8080:8080"

  postgres:
    image: postgres:17
    hostname: postgres
    container_name: postgres
    restart: always
    logging: *default_logging
    environment:
      - POSTGRES_USER=tg-spam
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=tg-spam
    volumes:
      - pg-data:/var/lib/postgresql/data

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: unless-stopped
    depends_on:
      - tg-spam
    env_file:
      - .env.tg-spam.local
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - ${CLOUDFLARED_TOKEN}

volumes:
  pg-data:
